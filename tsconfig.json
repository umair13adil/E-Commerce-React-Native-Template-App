{
  "extends": "@tsconfig/react-native/tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".", // all paths are relative to the baseUrl
    "paths": {
      "*": ["app/*"],
      "utils": ["app/utils/*"],
      "assets/*": ["assets/*"],
    }
  }
}
